@model IEnumerable<Modelos.Vehiculos.Marca>

@{
    ViewBag.Title = "Marcas";
}
<center>
    <h2>Marcas</h2>

    <p>
        @Html.ActionLink("Nueva Marca", "InsertarMarca", null, new { @class = "btn btn-success" })
    </p>
</center>
<div style="padding-left: 290px; padding-right: 290px">
    <table class="table">
        <thead class="thead-dark">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nombre)
                </th>
                <th></th>
            </tr>

        </thead>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.ActionLink("Editar", "ModificarMarca", new { id = item.ID }, new { @class = "btn btn-warning" }) |
                    @*@Html.ActionLink("Detalles", "Details", new { id = item.ID }, new { @class = "btn btn-info" }) |*@
                    @Html.ActionLink("Eliminar", "EliminarMarca", new { id = item.ID }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }

    </table>
</div>
